/**
 * Интерфейс координат
 */
export interface ICoordModel {
  latitude: number;
  longitude: number;
}

/**
 * Интерфейс координат (в виде строки)
 */
export interface ICoordStrModel {
  latitude: number;
  longitude: number;
}
